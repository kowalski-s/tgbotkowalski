# Инструкция по настройке и запуску бота

## Шаг 1: Создание бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Придумайте имя для бота (например: "Мой Лид Бот")
4. Придумайте username (должен заканчиваться на `bot`, например: `my_lead_bot`)
5. **СОХРАНИТЕ ТОКЕН!** Он понадобится для `.env` файла
6. Формат токена: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

## Шаг 2: Получение вашего Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте `/start`
3. Бот покажет ваш ID (например: `123456789`)
4. **СОХРАНИТЕ ID!** Это будет ваш ADMIN_ID

## Шаг 3: Настройка канала

### Вариант А: Использовать @username канала

1. Убедитесь, что у вашего канала есть публичный username
2. Формат: `@your_channel`
3. Добавьте бота в администраторы канала с правом "Просмотр сообщений"

### Вариант Б: Использовать числовой ID канала

1. Перешлите любое сообщение из канала боту [@username_to_id_bot](https://t.me/username_to_id_bot)
2. Бот покажет ID канала (формат: `-1001234567890`)
3. Добавьте бота в администраторы канала с правом "Просмотр сообщений"

**ВАЖНО:** Без прав администратора бот не сможет проверять подписку!

## Шаг 4: Настройка проекта

### 4.1 Клонирование и установка зависимостей

```bash
# Перейдите в папку проекта
cd D:\github\tgbotkowalski

# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
# Windows (cmd):
venv\Scripts\activate.bat

# Windows (PowerShell):
venv\Scripts\Activate.ps1

# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 4.2 Создание .env файла

```bash
# Windows:
copy .env.example .env

# Linux/Mac:
cp .env.example .env
```

Откройте файл `.env` в текстовом редакторе и заполните:

```env
# Токен от BotFather
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Ваш Telegram ID от @userinfobot
ADMIN_ID=123456789

# ID канала (с @ или числовой с минусом)
CHANNEL_ID=@your_channel
# или
CHANNEL_ID=-1001234567890

# Ссылка на канал для пользователей
CHANNEL_LINK=https://t.me/your_channel

# Ссылка на вашу статью
ARTICLE_LINK=https://example.com/your-article

# Остальное можно оставить по умолчанию
PDF_FILE_PATH=./data/bonus.pdf
DATABASE_PATH=./data/bot.db
```

**ВАЖНО:** Не публикуйте `.env` файл в git! Он уже добавлен в `.gitignore`.

### 4.3 Добавление PDF файла

Поместите ваш PDF файл в папку `data/` с именем `bonus.pdf`:

```bash
# Скопируйте ваш файл
copy "C:\path\to\your\file.pdf" "data\bonus.pdf"

# Или переименуйте и обновите .env:
PDF_FILE_PATH=./data/мой_бонус.pdf
```

## Шаг 5: Первый запуск (локально)

```bash
# Убедитесь, что виртуальное окружение активно
# Должно быть видно (venv) в начале строки

# Запустите бота
python -m bot.main
```

Если все настроено правильно, вы увидите:
```
✅ Бот запущен: @your_bot_username
📊 База данных: ./data/bot.db
```

И получите сообщение в Telegram от бота: "🤖 Бот успешно запущен!"

### Тестирование

1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Нажмите "Подписаться на канал"
4. Подпишитесь
5. Нажмите "Я подписался"
6. Должны получить ссылку и PDF файл

### Остановка бота

Нажмите `Ctrl+C` в терминале.

## Шаг 6: Деплой на VPS (cloud.ru)

### 6.1 Подготовка сервера

```bash
# Подключитесь к серверу по SSH
ssh user@your-server-ip

# Обновите систему
sudo apt update && sudo apt upgrade -y

# Установите Docker и Docker Compose
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo apt install docker-compose -y

# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER

# Перелогиньтесь для применения изменений
exit
# Подключитесь заново
```

### 6.2 Клонирование проекта

```bash
# Клонируйте репозиторий
git clone https://github.com/your-username/tgbotkowalski.git
cd tgbotkowalski

# Создайте .env файл
nano .env
# Вставьте все ваши настройки и сохраните (Ctrl+O, Enter, Ctrl+X)
```

### 6.3 Загрузка PDF файла

```bash
# Вариант 1: Скопируйте с локального компьютера через scp
# На локальном компьютере:
scp data/bonus.pdf user@your-server-ip:/home/user/tgbotkowalski/data/

# Вариант 2: Загрузите через wget
cd data/
wget https://example.com/path/to/your/file.pdf -O bonus.pdf
```

### 6.4 Запуск через Docker Compose

```bash
# Соберите и запустите контейнер
docker-compose up -d

# Проверьте логи
docker-compose logs -f

# Должны увидеть: "✅ Бот запущен"
# Нажмите Ctrl+C для выхода из логов (контейнер продолжит работать)
```

### 6.5 Управление ботом

```bash
# Просмотр статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f

# Остановка бота
docker-compose down

# Перезапуск после изменений
docker-compose restart

# Обновление кода
git pull
docker-compose up -d --build
```

## Шаг 7: Использование dokploy (опционально)

Если у вас установлен dokploy:

1. Откройте веб-интерфейс dokploy
2. Создайте новое приложение
3. Выберите "Deploy from Git"
4. Укажите URL вашего репозитория
5. Добавьте переменные окружения в интерфейсе:
   - `BOT_TOKEN`
   - `ADMIN_ID`
   - `CHANNEL_ID`
   - и т.д.
6. Нажмите "Deploy"

## Проверка работоспособности

### Команды бота

**Для всех пользователей:**
- `/start` - запустить бота

**Для администратора:**
- `/stats` - посмотреть статистику
- Reply на сообщение пользователя - ответить ему

### Ожидаемое поведение

1. Пользователь пишет `/start`
2. Получает сообщение с кнопками
3. Нажимает "Подписаться" → открывается канал
4. Подписывается
5. Нажимает "Я подписался"
6. Получает: ссылку, PDF, сообщение о работе
7. Пишет сообщение в бота
8. Администратор получает уведомление
9. Администратор отвечает через Reply
10. Пользователь получает ответ

## Возможные проблемы

### Ошибка: "BOT_TOKEN не установлен"
- Проверьте, что `.env` файл существует
- Проверьте, что в `.env` есть строка `BOT_TOKEN=...`

### Ошибка при проверке подписки
- Убедитесь, что бот добавлен в администраторы канала
- Проверьте правильность `CHANNEL_ID` (с @ или с минусом для числового ID)

### PDF файл не отправляется
- Проверьте, что файл существует: `ls -la data/bonus.pdf`
- Проверьте путь в `.env`: `PDF_FILE_PATH=./data/bonus.pdf`

### Бот не отвечает
- Проверьте логи: `docker-compose logs -f`
- Проверьте, что контейнер запущен: `docker-compose ps`

### Не приходят уведомления администратору
- Проверьте правильность `ADMIN_ID` в `.env`
- Запустите бота и отправьте `/start` от своего аккаунта

## Мониторинг

```bash
# Просмотр использования ресурсов
docker stats

# Просмотр логов за последние 100 строк
docker-compose logs --tail=100

# Постоянный мониторинг логов
docker-compose logs -f
```

## Резервное копирование

База данных находится в `data/bot.db`. Регулярно делайте бэкап:

```bash
# Создание бэкапа
cp data/bot.db data/bot.db.backup_$(date +%Y%m%d)

# Или автоматически через cron (каждый день в 3:00)
0 3 * * * cp /path/to/tgbotkowalski/data/bot.db /path/to/backups/bot.db.$(date +\%Y\%m\%d)
```

## Поддержка

- Подробная документация: `claude.md`
- Описание проекта: `README.md`
- Логи бота: `bot.log` (при локальном запуске)

Удачного запуска! 🚀
