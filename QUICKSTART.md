# Быстрый старт

## 1. Получите данные от Telegram

1. **Токен бота:** [@BotFather](https://t.me/BotFather) → `/newbot`
2. **Ваш ID:** [@userinfobot](https://t.me/userinfobot) → `/start`
3. **ID канала:** [@username_to_id_bot](https://t.me/username_to_id_bot) → перешлите сообщение из канала
4. **Добавьте бота в админы канала** с правом "Просмотр сообщений"

## 2. Настройте проект

```bash
# Установите зависимости
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt

# Создайте .env
copy .env.example .env  # Windows
# Отредактируйте .env и укажите токен, ID, канал

# Добавьте PDF файл
# Скопируйте ваш bonus.pdf в папку data/
```

## 3. Проверьте настройки

```bash
# Проверьте конфигурацию
python check_config.py

# ВАЖНО: Проверьте доступ к каналу
python test_channel.py
```

Если test_channel.py показывает ошибки - следуйте инструкциям в [TROUBLESHOOTING.md](TROUBLESHOOTING.md)

## 4. Запустите

```bash
python -m bot.main
```

## 5. Тестируйте

1. Откройте бота в Telegram
2. `/start`
3. **Если вы УЖЕ подписаны** - сразу получите материалы!
4. Если нет - подпишитесь на канал
5. Нажмите "Я подписался"
6. Получите материалы!

## ❌ Не работает проверка подписки?

Смотрите [TROUBLESHOOTING.md](TROUBLESHOOTING.md)

## Команды

- `/start` - для всех пользователей
- `/stats` - статистика (только админ)
- Reply на сообщение - ответить пользователю (только админ)

## Деплой на VPS

```bash
# На сервере
git clone <repo-url>
cd tgbotkowalski
nano .env  # Вставьте настройки
docker-compose up -d
```

Готово! 🎉

Подробная инструкция: [SETUP.md](SETUP.md)
